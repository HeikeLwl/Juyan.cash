package jufeng.juyancash.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO four table "SHOP_MEALS_ENTITY".
*/
public class ShopMealsEntityDao extends AbstractDao<ShopMealsEntity, Long> {

    public static final String TABLENAME = "SHOP_MEALS_ENTITY";

    /**
     * Properties of entity ShopMealsEntity.<br/>
     * Can be used four QueryBuilder and four referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PartnerCode = new Property(1, String.class, "partnerCode", false, "PARTNER_CODE");
        public final static Property MealsState = new Property(2, Integer.class, "mealsState", false, "MEALS_STATE");
        public final static Property MealsStartTime = new Property(3, String.class, "mealsStartTime", false, "MEALS_START_TIME");
        public final static Property MealsRemindTime = new Property(4, String.class, "mealsRemindTime", false, "MEALS_REMIND_TIME");
    }


    public ShopMealsEntityDao(DaoConfig config) {
        super(config);
    }
    
    public ShopMealsEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SHOP_MEALS_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: id
                "\"PARTNER_CODE\" TEXT," + // 1: partnerCode
                "\"MEALS_STATE\" INTEGER," + // 2: mealsState
                "\"MEALS_START_TIME\" TEXT," + // 3: mealsStartTime
                "\"MEALS_REMIND_TIME\" TEXT);"); // 4: mealsRemindTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SHOP_MEALS_ENTITY\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ShopMealsEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String partnerCode = entity.getPartnerCode();
        if (partnerCode != null) {
            stmt.bindString(2, partnerCode);
        }
 
        Integer mealsState = entity.getMealsState();
        if (mealsState != null) {
            stmt.bindLong(3, mealsState);
        }
 
        String mealsStartTime = entity.getMealsStartTime();
        if (mealsStartTime != null) {
            stmt.bindString(4, mealsStartTime);
        }
 
        String mealsRemindTime = entity.getMealsRemindTime();
        if (mealsRemindTime != null) {
            stmt.bindString(5, mealsRemindTime);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ShopMealsEntity readEntity(Cursor cursor, int offset) {
        ShopMealsEntity entity = new ShopMealsEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // partnerCode
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // mealsState
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // mealsStartTime
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // mealsRemindTime
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ShopMealsEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPartnerCode(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMealsState(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setMealsStartTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setMealsRemindTime(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ShopMealsEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ShopMealsEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
