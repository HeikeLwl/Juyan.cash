package jufeng.juyancash.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "MATERIAL_ENTITY".
*/
public class MaterialEntityDao extends AbstractDao<MaterialEntity, Long> {

    public static final String TABLENAME = "MATERIAL_ENTITY";

    /**
     * Properties of entity MaterialEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MaterialId = new Property(1, String.class, "materialId", false, "MATERIAL_ID");
        public final static Property MaterialName = new Property(2, String.class, "materialName", false, "MATERIAL_NAME");
        public final static Property MaterialPrice = new Property(3, Integer.class, "materialPrice", false, "MATERIAL_PRICE");
    };


    public MaterialEntityDao(DaoConfig config) {
        super(config);
    }
    
    public MaterialEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MATERIAL_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: id
                "\"MATERIAL_ID\" TEXT," + // 1: materialId
                "\"MATERIAL_NAME\" TEXT," + // 2: materialName
                "\"MATERIAL_PRICE\" INTEGER);"); // 3: materialPrice
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MATERIAL_ENTITY\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, MaterialEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String materialId = entity.getMaterialId();
        if (materialId != null) {
            stmt.bindString(2, materialId);
        }
 
        String materialName = entity.getMaterialName();
        if (materialName != null) {
            stmt.bindString(3, materialName);
        }
 
        Integer materialPrice = entity.getMaterialPrice();
        if (materialPrice != null) {
            stmt.bindLong(4, materialPrice);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public MaterialEntity readEntity(Cursor cursor, int offset) {
        MaterialEntity entity = new MaterialEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // materialId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // materialName
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3) // materialPrice
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, MaterialEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMaterialId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMaterialName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setMaterialPrice(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(MaterialEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(MaterialEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
