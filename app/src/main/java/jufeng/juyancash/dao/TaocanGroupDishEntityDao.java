package jufeng.juyancash.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO four table "TAOCAN_GROUP_DISH_ENTITY".
*/
public class TaocanGroupDishEntityDao extends AbstractDao<TaocanGroupDishEntity, Long> {

    public static final String TABLENAME = "TAOCAN_GROUP_DISH_ENTITY";

    /**
     * Properties of entity TaocanGroupDishEntity.<br/>
     * Can be used four QueryBuilder and four referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property TaocanGroupDishId = new Property(1, String.class, "taocanGroupDishId", false, "TAOCAN_GROUP_DISH_ID");
        public final static Property DishId = new Property(2, String.class, "dishId", false, "DISH_ID");
        public final static Property TaocanGroupId = new Property(3, String.class, "taocanGroupId", false, "TAOCAN_GROUP_ID");
        public final static Property SpecifyId = new Property(4, String.class, "specifyId", false, "SPECIFY_ID");
        public final static Property IncreasePrice = new Property(5, Float.class, "increasePrice", false, "INCREASE_PRICE");
        public final static Property SelectDishCount = new Property(6, Integer.class, "selectDishCount", false, "SELECT_DISH_COUNT");
    }


    public TaocanGroupDishEntityDao(DaoConfig config) {
        super(config);
    }
    
    public TaocanGroupDishEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TAOCAN_GROUP_DISH_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: id
                "\"TAOCAN_GROUP_DISH_ID\" TEXT NOT NULL UNIQUE ," + // 1: taocanGroupDishId
                "\"DISH_ID\" TEXT," + // 2: dishId
                "\"TAOCAN_GROUP_ID\" TEXT," + // 3: taocanGroupId
                "\"SPECIFY_ID\" TEXT," + // 4: specifyId
                "\"INCREASE_PRICE\" REAL," + // 5: increasePrice
                "\"SELECT_DISH_COUNT\" INTEGER);"); // 6: selectDishCount
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TAOCAN_GROUP_DISH_ENTITY\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TaocanGroupDishEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getTaocanGroupDishId());
 
        String dishId = entity.getDishId();
        if (dishId != null) {
            stmt.bindString(3, dishId);
        }
 
        String taocanGroupId = entity.getTaocanGroupId();
        if (taocanGroupId != null) {
            stmt.bindString(4, taocanGroupId);
        }
 
        String specifyId = entity.getSpecifyId();
        if (specifyId != null) {
            stmt.bindString(5, specifyId);
        }
 
        Float increasePrice = entity.getIncreasePrice();
        if (increasePrice != null) {
            stmt.bindDouble(6, increasePrice);
        }
 
        Integer selectDishCount = entity.getSelectDishCount();
        if (selectDishCount != null) {
            stmt.bindLong(7, selectDishCount);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TaocanGroupDishEntity readEntity(Cursor cursor, int offset) {
        TaocanGroupDishEntity entity = new TaocanGroupDishEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // taocanGroupDishId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // dishId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // taocanGroupId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // specifyId
            cursor.isNull(offset + 5) ? null : cursor.getFloat(offset + 5), // increasePrice
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6) // selectDishCount
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TaocanGroupDishEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTaocanGroupDishId(cursor.getString(offset + 1));
        entity.setDishId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTaocanGroupId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setSpecifyId(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setIncreasePrice(cursor.isNull(offset + 5) ? null : cursor.getFloat(offset + 5));
        entity.setSelectDishCount(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TaocanGroupDishEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TaocanGroupDishEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
